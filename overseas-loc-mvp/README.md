# 出海出稿（8787）

竞品库生成脚本后，在此页 **完成交付** → 下载 zip。

**交付 zip**：`交付脚本包.md/json` + `subtitles.srt` + `剪辑单.html`（+ SeedDance 空镜如有）

启动：双击 `start.bat` 或根目录 `启动工作台.cmd`  
完整说明见仓库根目录 `README_使用说明.md`

配置：复制 `.env.example` → `.env`，填写 `FAL_KEY`（SeedDance）
